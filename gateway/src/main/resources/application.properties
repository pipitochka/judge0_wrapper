# ?????? ??? ??????????? ? PostgreSQL
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://gateway-db:5432/gateway_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:gateway_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:gateway_password}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ???????????: ????????? ???? HikariCP
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1800000

app.scibox.api-key=${SCIBOX_API_KEY}
app.judge0.url=${JUDGE0_URL:http://judge0-server:2358}
app.scibox.url=${SCIBOX_URL:https://llm.t1v.scibox.tech/v1}
